# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Local SQLite database (regenerated by the pipeline)
*.db

# OS noise
.DS_Store

# Note: data/*.csv and output/* (charts + scorecard) ARE committed so the
# repo is fully viewable without running the pipeline.
